services:
  - type: web
    name: neurograde-ai
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y tesseract-ocr
    startCommand: python Assignment.py